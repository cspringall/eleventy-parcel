{% if collections.posts.length %}
<section>
    <ul>
        {% for post in collections.posts %}
        <li>
            <h3><a href="{{ post.url }}">{{ post.data.pageTitle }}</a></h3>
            <p>{{ post.date }}</p>
        </li>
        {% endfor %}
    </ul>
</section>
{% endif %}